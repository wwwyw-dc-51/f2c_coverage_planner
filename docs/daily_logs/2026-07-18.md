# 2026-07-18 工作日志

## v9.6 剩余问题本地修复 (Codex)

以 `7aaab75` 为本轮起点，完成 5 个独立修复提交。

| 提交 | 内容 | 对应问题 |
|------|------|----------|
| `7a10387` | 覆盖门控和当前批测脚本统一为 99% | 99% 满分与 99.5% 通过阈值不一致 |
| `57f4744` | audit 默认机器人宽度改为 0.75 m，并新增 `--merge-distance` | audit 参数落后于当前基线 |
| `e80cef0` | `output_dir` 默认使用操作系统临时目录 | `/tmp` 硬编码导致 Windows 不兼容 |
| `6e0c7ea` | 分离 0.90 m 覆盖宽度与 0.873 m 有效行距 | S3 边界补线出现约 1.35 cm 相位偏差 |
| `151391b` | 单 Cell 窄矩形中的近重合 swath 改为边界锚定等距重排 | S4 四条轨迹中存在两对近重合线 |

## Claude 交叉审计 + VM 验收

- 交叉审计：6 commits 全部通过，无阻塞问题
- 发现测试用例 `UsesCoverageWidthRatherThanRowSpacingForBoundaryOffset` 左侧断言不可达（主 swath 已覆盖边界）
- Codex 给出修复方案，Claude 实施 → commit `dc684e5`
- VM 验证：47/47 gtest 通过，7/7 batch test 通过

## v9.7 7 场景基准 (batch_0718_1633)

| 场景 | 覆盖率 | 得分 | vs v9.6 |
|------|:------:|:----:|:--:|
| S1 | 100.00% | 97.2 | +1.5 |
| S2 | 100.00% | 96.8 | +1.7 |
| S3 | 99.97% | 80.8 | +0.7 |
| S4 | 100.00% | 89.2 | **+13.8** |
| S5 | 100.00% | 84.7 | +2.7 |
| S6 | 100.00% | 72.7 | +0.5 |
| notched | 99.99% | 81.8 | -2.8 |
| **平均** | **99.99%** | **86.2** | **+2.6** |

### S4 确认
- path_pts=6 → 3 条等距 swath，重排生效
- 重叠率 77.9% → 29.1% (-48.8pp)

### S3 确认
- 覆盖率 99.60% → 99.97%
- 重叠率 19.6% → 15.7% (-3.9pp)

### S6 确认
- 历史首次 100% 覆盖

## 版本推进
- v9.6 → **v9.7**
- 已知问题清理：门控阈值、audit 参数、/tmp 硬编码、S3 去重、S4 近重合 → 全部已修复
- 测试数：24 → 47 (+23)
- 新增模块：artifact_sink.hpp/cpp（平台临时目录接口）

## GPT 独立审计 — 待处理

### P0: 批测门控加固（阻塞 v9.7 正式验收）

当前 `batch_test_v2.sh` 的 `COVERAGE_THRESHOLD=0.99` 从未实际使用，通过条件只检查 data.json 是否存在。
需修复：

1. **覆盖率门控生效**：读取每个场景 `_data.json` 的 `eval.coverage_rate`，低于 0.99 则标记失败并设置非零退出码
2. **路径有效性检查**：验证路径非空（`path` 数组长度 > 0），空路径应视为失败
3. **残留文件清理**：每场景开始前删除 `/tmp/f2c_vis_polygon_*.json` 和 grid 文件，防止跨场景污染
4. **超时保护**：eval 超时后不应生成空 data.json，或生成后应明确标记为无效
5. **集成 audit**：每个场景完成后立即运行 `audit_test_report.py`，将发现汇总到最终报告

### P1: FOOTPRINT_CLEARANCE 净空违规（下版本处理）

- 来源：`fillBoundaryGaps` 的 `robot_half_width` 参数被显式忽略，边界补线仅按 coverage_width 定位
- 影响：closed 边界模式下 7 场景全部存在净空违规，端点距边界仅 ~0.15m（需要 ≥0.375m）
- 决策：已知设计权衡，本轮不修。新建 issue 追踪，v9.8 评估
