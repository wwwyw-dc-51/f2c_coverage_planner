# 2026-07-18 工作日志

## v9.6 剩余问题本地修复

以 `7aaab75` 为本轮起点，完成 5 个独立修复提交。

| 提交 | 内容 | 对应问题 |
|------|------|----------|
| `7a10387` | 覆盖门控和当前批测脚本统一为 99% | 99% 满分与 99.5% 通过阈值不一致 |
| `57f4744` | audit 默认机器人宽度改为 0.75 m，并新增 `--merge-distance` | audit 参数落后于当前基线 |
| `e80cef0` | `output_dir` 默认使用操作系统临时目录 | `/tmp` 硬编码导致 Windows 不兼容 |
| `6e0c7ea` | 分离 0.90 m 覆盖宽度与 0.873 m 有效行距 | S3 边界补线出现约 1.35 cm 相位偏差 |
| `151391b` | 单 Cell 窄矩形中的近重合 swath 改为边界锚定等距重排 | S4 四条轨迹中存在两对近重合线 |

### S3 归因

- `batch_0717_1734` 的最终路径重叠率为 17.45%，只计算 swath 时约为 13.95%；连接段约贡献 3.5 个百分点。
- 边界补线原来错误使用有效行距 `0.90 × (1 - 3%) = 0.873 m` 计算偏移，而不是实际覆盖宽度 0.90 m。
- 错误偏移为 `0.90 / 2 - 0.873 / 2 = 0.0135 m`，与 S3 中多组约 0.016 m 的近重合轨迹一致。
- 旧 ROS 管线和 PlannerCore 管线已同时改为：有效行距只用于主 swath 生成，实际覆盖宽度用于边界补线和接缝去重。

### S4 归因与修复

- S4 只有一个 Cell，排除相邻 Cell 接缝重复。
- 历史四条纵向轨迹横坐标为 `1.5635, 1.4595, 0.5865, 0.4365`，两对间距仅 0.104 m 和 0.150 m。
- 新策略只在单 Cell、无孔洞、矩形、窄区域、等长平行 swath 且确有近重合线时触发。
- 对 S4 的离线几何计算将轨迹重排为 `1.55, 1.00, 0.45`；覆盖率保持 100%，纯 swath 重叠估算由 80.0% 降至约 35.0%。

### notched 与门控

- 历史 notched 覆盖率为 99.68%，已经高于产品 99% 阈值。
- 本轮统一门控后，该场景会获得完整覆盖门控分，不再处于 99%/99.5% 灰色区间；未为追求 100% 额外增加路径。

## 本地验证

- `python -m unittest test_audit_test_report.py -v`：15/15 通过。
- Python 语法检查：通过。
- `batch_test_v2.sh`、`run_f2c_optimized.sh` Bash 语法检查：通过。
- Visual Studio 2022 MSVC C++17 冒烟测试：跨平台临时目录接口编译并运行通过。
- `git diff --check 7aaab75...HEAD`：通过。
- 工作树：干净。

## VM 待验收

Windows 本机没有 ROS2 Humble 和 Fields2Cover Linux 构建依赖。代码同步到 VM 后执行：

```bash
cd ~/f2c_coverage_planner
colcon build --packages-select yingshi_robot --cmake-args -DYINGSHI_ENABLE_EVALUATION=ON
source install/setup.bash
colcon test --packages-select yingshi_robot --event-handlers console_direct+
colcon test-result --verbose
bash scripts/batch_test_v2.sh
```

重点验收：

1. 全部 C++ 测试通过，包括新增的覆盖宽度偏移、窄矩形重排和临时目录测试。
2. S4 输出 3 条纵向 swath，覆盖率仍为 100%，重叠率显著低于 77.9%。
3. S3 覆盖率不低于 99%，重叠率低于 v9.6 的 19.6% 基线。
4. S1、S2、S5、S6、notched 无覆盖率或安全性回归。
