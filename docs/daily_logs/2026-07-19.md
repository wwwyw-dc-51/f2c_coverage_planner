# 2026-07-19 工作日志

## v9.8 — C-space 退役 + 空隙分类器 + Phase 4A

### 背景
v9.7 回滚后 C-space 被标记为待删除。老板决定用空隙分类器替代：规划后分析未覆盖区域，区分"物理不可达"和"算法可改进"。

### 改动

#### C-space 退役
- `traversability_enabled` 默认 `false`
- `batch_test_v2.sh`、`run_f2c_optimized.sh`、`run_f2c_rviz_demo.sh` 移除参数

#### 安全门容差
- `path_sanity_check.hpp`：边界浮点 WARN 不阻断，真正越界 ERROR
- S7 之前被安全门拒绝（1 path point outside polygon），定位到 `(29.625, 19.6249)` 恰好在 C-space 收缩后的右边界上

#### Phase 4A 加回
- 从已回退的 `f08144c` cherry-pick `planner_core.cpp` + `swath_generator.cpp` + 对应 headers
- sweep alignment + adaptive headland + slant detection
- 修复了退化：S5 99.03%→100%，S6 99.47%→100%

#### 空隙分类器
- `coverage_evaluator.hpp` 新增 `classifyUncoveredGaps()`
- 连通分量 + 最大内切宽度 vs robot_width
- 修正覆盖率 = covered / (target - unreachable)
- S7 找到 15 处物理不可达空隙 (0.59m²)

#### 双覆盖率报告
- `batch_test_v2.sh` 报告新增"修正覆盖率"列
- `batch_result_gate.py` C-space 字段为空时跳过检查

#### 渲染修复
- `render_coverage.py`：包围盒裁剪 + 5% margin（旧图 98% 空白→30%）
- cell alpha 0.35→0.6，connections cell alpha 0.15→0.6

#### Vis JSON 修复
- `polygon_planner_node.cpp`：cells 改用 `component.decomposition_cells` 而非 `planning_polygon`
- S7 cells: 1→19, connections: 0→124

### 验证
- gtest: 74/74 全部通过
- batch: 7/8 GATE PASS (S7 98.07% 低于 99% 阈值)
- CRLF 行尾问题：`sed -i 's/\r//'` 修复 batch_test_v2.sh
- 修复了 `planner_core_test`：golden baseline 48→52 swaths，PreservesDisconnectedAreasAsSeparateSubpaths 加边界容差

### 待办
- S7 斜线路径优化（cell 间 direct 直连→A* 自由空间路由）
- FOOTPRINT_CLEARANCE

---

## v9.9 — 微 cell 合并优化 + S7 深度分析

### P1-A：filter/merge 顺序调换 ✅ 已提交
- **commit**: `8857a46`
- **改动**：`planner_core.cpp` + `polygon_planner_node.cpp` 中 `filterTinyCells` 移到 `mergeCellsWithSimilarDirection` 之后
- **原因**：旧逻辑微 cell 在合并前就被丢弃，无法被邻居吸收 → 覆盖率损失
- **效果**：S7 覆盖率 98.07%→98.42%（+0.35%），得分 41.0→47.7（+6.7）
- **其他场景**：零退化

### P0：A* 自由空间路由 🚧 开发中
- **方案**：`repairConnectionPolyline` 中用 A* 网格搜索替代 boundary-walk
- **状态**：本地 gtest HoleAwareRoute 测试通过，VM 上 S7 批测崩溃（SIGSEGV）
- **定位**：崩溃在 A* 循环 > 1000 次迭代后，10/100 次 OK
- **疑因**：VM 升级后环境差异
- **未提交**

### P1-B：孔洞间窄通道检测 🚧 暂存
- **方案**：扩展 `min_passage` 到 hole-to-hole 和 hole-to-boundary
- **状态**：VM 上同样崩溃，已从代码中移除

### S7 分析
- 五大瓶颈：跨 cell 对角线（47%重叠）、微 cell 碎片、窄通道侵蚀、boundary_filler 偏移、全局 swath 角度
- P0（A* 路由）预期最有效：重叠 47%→30%，得分 41→55+

### 基础设施
- sync_and_build.sh 补了 `test_polygons/*.yaml` 同步
- 新增 `scripts/s7_quick_test.sh` 单场景快速迭代脚本
