# 2026-07-09 工作日志

## 上午（会话1 — 意外关闭前）

### 改动
- 候选 headland 扫描 + 边界检查 + 否决：编译通过但越界检测在 RDP 后漏检
- 修复：RDP 前检测，确认根因：OOB 100% TURN 段，集中在 x≈6 瓶颈
- GPT 确认：几何预处理正常，问题在 Turn

### 下午早期
- 窄 cell ring 替换（只标记 cell 2,3）→ 漏检，回退
- 全量 ring 替换（全部 TURN → 沿 cell 外环）→ 路径全乱，回退
- Cell 合并启用（45°/0.9m）+ simplifyRing 5°
- `regularizeOrthogonal` 尝试 → 有 bug，回退（代码中已不存在）
- W1 不分解模式测试（`decomposition_enabled:=false`）
- 调研文献：F2C #150（自交环不是我们的问题）、MDPI 2025 区域过渡
- DubinsCC 支持（加了代码，代码中实际未找到→可能未提交）
- **最后改动未编译测试即意外关闭**

## 下午（会话2 — 当前会话）

### 核心发现
1. Cell 不穿越孔洞 — rectilinear 分解正确排除孔洞区域，12个cell全部 rings=1
2. 真正的穿越在路径层 — 3个详细路径线段穿过孔洞，是 direct turn 连接
3. GDAL `difference(polygon_with_hole, hole)` 永远保留内环 — OGC 标准

### 尝试方案
- ❌ 预减法 → GDAL 保留内环
- ❌ 后减法 → 0 affected（cell本就不跨孔洞）
- ❌ W1不分解 → 不适用此场景
- ❌ 移除孔洞顶点网格线 → 3大cell含donut，跨孔洞连接仍存在
- ❌ 绕行后处理（方向C）→ 用户不接受打补丁
- 🟡 **方向B + 全局swath优化** → 已写未测

### 代码改动（会话2）
- `polygon_planner_node.cpp`:
  - Cell几何诊断（面积/bbox/顶点数/非矩形标记/退化顶点打印）
  - 路径孔洞三重检测（点级+线段级+详细路径）
  - 全局swath角度优化（替代逐cell局部最优）
  - 孔洞感知swath重排（方向B：holeSide贪婪交换）
  - Cell合并启用（移除 `false &&`）
  - `#include <iomanip>`, `#include <utility>`
- `run_f2c_optimized.sh`:
  - LD_LIBRARY_PATH加steering_functions-build
  - 诊断日志自动重定向到diag_logs/

### 待办
- [ ] 编译测试方向B + 全局优化
- [ ] 检查 Hole-aware reorder 是否触发
- [ ] 检查 DETAILED PATH 穿越是否消除
- [ ] 清理死代码（后减法循环、绕行补丁残留）
