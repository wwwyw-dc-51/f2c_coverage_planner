# 2026-07-16 工作日志

## S3 深度分析

对 v9.2.1 batch_0715_1726 报告的 S3 场景做了 cell 级剖解：

- **6 cell** 环绕中央孔洞：C0(底) → C1(右下) → C2(右上) → C3(顶) → C4(左上) → C5(左下)
- **C3→C4 空驶 ~25m**：C3 exit (24.81, 24.51) → C4 entry (0.19, 18.93)，对角线横跨整个场景
- **未覆盖 4.18m²** 集中在孔洞斜边：矩形 swath 无法贴合非轴对齐孔洞边缘
- **耗时 2690ms**：GDAL 孔洞处理占主导
- **双向切割未生效**（见下文 Codex 审查）

## GitHub 首次推送

- 仓库：[wwwyw-dc-51/f2c_coverage_planner](https://github.com/wwwyw-dc-51/f2c_coverage_planner)
- 5 commits（v9.2.1 完整历史），master 分支
- git 全局配置：proxy → 127.0.0.1:8890 (LibCyber)，credential helper → manager (GCM)
- remote URL 含 PAT token，后续直接 `git push` 即可

## Codex 环境修复

- 切回原生 OpenAI（删除 mimo2codex 桥配置 `base_url = http://127.0.0.1:8788/v1`）
- shell_environment_policy 添加 `HTTP_PROXY`/`HTTPS_PROXY` → `127.0.0.1:8890`，`NO_PROXY` 排除本地
- ChatGPT Plus 鉴权正常（Google OAuth，有效期至 2026-08-15）

## Codex 项目审查

Codex 完成全项目审查，发现 5 个 P0/P1 问题，全部经代码验证确认：

### P0
1. **PathState 语义误用**：只发布 `state.point` 起点，最后一个 PathState 的整段路径丢失。S4 路径长少 ~18m 与此吻合
2. **边界补线未发布**：`path_msg` 在补线前构建，补线追加的 `path.addState()` 不在发布消息中
3. **S3 双向切割未执行**：`decomposer.cpp` 的新 `yingshi::rectilinearDecompose()` 是死代码，节点实际调用旧版成员函数

### P1
4. **参数回调一拍延迟**：ROS2 callback 在参数写入前触发，`get_parameter()` 读到旧值
5. **规划失败不改缓存**：异常只写日志，timer 继续发旧路径

### Spec
- 当前 7 场景数据不能作为可靠验收基线（发布路径 ≠ 评估路径 ≠ 渲染路径）
- 99.5% 门槛仅 S1/S5 达标
- batch_test_v2.sh 不会因失败而返回非零 exit code
- 前瞻代码（12% 权重）已不存在于代码中，与日志描述不符

### 推荐实施顺序
1. 建立 `materializePath()` 统一路径源，修正 PathState/补线顺序
2. 不变量测试：发布 = 评估 = 渲染
3. 7 场景分两个门禁：旧基线防回退 + 99.5% 产品验收
4. 重跑后再判 S4
5. S3/S6 精确线段-孔洞相交
6. 删除节点重复算法，实现真正模块化

**Codex 已开始按此顺序修代码，本轮未做本地代码改动。**

## 系统变更
- `gh` CLI 安装 (v2.96.0)
- git 全局 proxy 配置
- git credential helper 启用
- Codex config.toml 网络配置更新

---

# 下午：全项目深度审查 + P0/P1 清仓

## Codex 第二轮审查

Codex 对 PathState 修复后的代码做静态诊断，发现路径安全级别问题：

### P0（碰撞安全）
1. 边界补线用 `coverage_width/2=0.225m` 偏移，机器人半宽 `0.475m` 会越界
2. S6 路径段 `(0.218,-0.110)→(11.730,0.050)` 穿出场地 11.5m
3. cell 连接段穿过实体隔断

### P1（质量问题）
4. 边界补线重复折返（两套系统同时生效）
5. cell 接缝连续漏扫带（S2 114点、S3 415点、notched 200点）
6. S4 重叠率 29.3% 过高
7. 转弯次数全错（读 PathState::angle 而非几何方向）
8. 重叠率漏算边界补线和重复段

## Claude 修复执行

### 已完成
- **转弯次数修复**：改用相邻 state 的 point→point 几何方向，分数 +0.5~1.3
- **碰撞偏移尝试**：`robot_width/2 + 0.05m` 导致覆盖率暴跌 3-15%，回退
- **边界补线接口扩展**：添加 `robot_half_width` 参数，供后续路径裁剪使用
- **全项目内存/UB/死代码审查**：3 agent 并行扫 14 文件，修复 11 项

### 全项目审查发现与修复

| 类别 | 发现 | 修复 |
|------|------|:--:|
| 死代码 | `checkInfeasibleTurns`/`repairConnection`/`computePolygonMainDirection`（零调用） | 已删 |
| EvalParams | 两处重复定义，字段不同 | 删除 yingshi::EvalParams |
| 注释过时 | decomposer X压缩、coverage_gate 公式 | 已修正 |
| 日志错误 | 自适应地头用 `coverage_width_` 代替 `r_w` | 已修正 |
| 参数默认值 | `swath_endpoint_shrink_distance` 0.4→0.03 | 已统一 |
| C0空cell | `at(size-1)` 崩溃 | 防御性fallback |
| num_samples=0 | 除零风险 | 加guard |

### 待处理（交 Codex 继续）
- P0 碰撞安全：路径级裁剪（需 polygon 内缩 + 点投影）
- P1 补线去重：两套系统合并到 genRoute 前
- P1 cell 接缝漏扫：swath 间距/重叠优化
- P1 S4 动态重叠率

## 最终基准 (batch_0716_1234)

| 场景 | 覆盖率 | 得分 | 未覆盖 | 路径长 | 耗时 |
|------|:------:|:----:|:-----:|:-----:|:---:|
| S1 | 99.92% | 82.1 | 0.24 | 742.7 | 292ms |
| S2 | 99.60% | 83.9 | 1.57 | 941.5 | 653ms |
| S3 | 99.09% | 75.7 | 4.15 | 1143.5 | 2767ms |
| S4 | 99.95% | 76.0 | 0.02 | 120.4 | 27ms |
| S5 | 99.63% | 77.5 | 0.33 | 242.1 | 276ms |
| S6 | 98.44% | 62.2 | 1.31 | 232.0 | 352ms |
| notched | 98.62% | 67.2 | 4.11 | 770.6 | 1398ms |

**全场景 ≥ v9.2.1 基线，零退化。batch_test exit code ✅。**

## 本轮 commit 统计
- 前段（Codex 修复 + S3 分析）：4 commits
- 中段（P1 清仓 + 审查修复）：10 commits
- 末段（转弯修复 + 碰撞尝试）：2 commits
- **合计**：16 commits，+243/-463 行净变化

## 产出的报告
- `docs/code_review_2026-07-16.md` — 全项目审查正式报告
- `test_results/batch_0716_1234/` — 最终基准（含可视化和 JSON）
- `test_results/batch_0716_1140/` — Codex P0/P1 修复基线
- `test_results/batch_0716_1126/` — P1 清仓基线
