# 2026-07-16 工作日志

## S3 深度分析

对 v9.2.1 batch_0715_1726 报告的 S3 场景做了 cell 级剖解：

- **6 cell** 环绕中央孔洞：C0(底) → C1(右下) → C2(右上) → C3(顶) → C4(左上) → C5(左下)
- **C3→C4 空驶 ~25m**：C3 exit (24.81, 24.51) → C4 entry (0.19, 18.93)，对角线横跨整个场景
- **未覆盖 4.18m²** 集中在孔洞斜边：矩形 swath 无法贴合非轴对齐孔洞边缘
- **耗时 2690ms**：GDAL 孔洞处理占主导
- **双向切割未生效**（见下文 Codex 审查）

## GitHub 首次推送

- 仓库：[wwwyw-dc-51/f2c_coverage_planner](https://github.com/wwwyw-dc-51/f2c_coverage_planner)
- 5 commits（v9.2.1 完整历史），master 分支
- git 全局配置：proxy → 127.0.0.1:8890 (LibCyber)，credential helper → manager (GCM)
- remote URL 含 PAT token，后续直接 `git push` 即可

## Codex 环境修复

- 切回原生 OpenAI（删除 mimo2codex 桥配置 `base_url = http://127.0.0.1:8788/v1`）
- shell_environment_policy 添加 `HTTP_PROXY`/`HTTPS_PROXY` → `127.0.0.1:8890`，`NO_PROXY` 排除本地
- ChatGPT Plus 鉴权正常（Google OAuth，有效期至 2026-08-15）

## Codex 项目审查

Codex 完成全项目审查，发现 5 个 P0/P1 问题，全部经代码验证确认：

### P0
1. **PathState 语义误用**：只发布 `state.point` 起点，最后一个 PathState 的整段路径丢失。S4 路径长少 ~18m 与此吻合
2. **边界补线未发布**：`path_msg` 在补线前构建，补线追加的 `path.addState()` 不在发布消息中
3. **S3 双向切割未执行**：`decomposer.cpp` 的新 `yingshi::rectilinearDecompose()` 是死代码，节点实际调用旧版成员函数

### P1
4. **参数回调一拍延迟**：ROS2 callback 在参数写入前触发，`get_parameter()` 读到旧值
5. **规划失败不改缓存**：异常只写日志，timer 继续发旧路径

### Spec
- 当前 7 场景数据不能作为可靠验收基线（发布路径 ≠ 评估路径 ≠ 渲染路径）
- 99.5% 门槛仅 S1/S5 达标
- batch_test_v2.sh 不会因失败而返回非零 exit code
- 前瞻代码（12% 权重）已不存在于代码中，与日志描述不符

### 推荐实施顺序
1. 建立 `materializePath()` 统一路径源，修正 PathState/补线顺序
2. 不变量测试：发布 = 评估 = 渲染
3. 7 场景分两个门禁：旧基线防回退 + 99.5% 产品验收
4. 重跑后再判 S4
5. S3/S6 精确线段-孔洞相交
6. 删除节点重复算法，实现真正模块化

**Codex 已开始按此顺序修代码，本轮未做本地代码改动。**

## 系统变更
- `gh` CLI 安装 (v2.96.0)
- git 全局 proxy 配置
- git credential helper 启用
- Codex config.toml 网络配置更新
