S3 扫描线分解方案 — 孔洞: 六边形(7,6)→(18,5)→(21,13)→(16,19)→(8,18)→(5,12)→回(7,6)
Swath 方向: 0° (水平)
分解原理: 从孔洞每个顶点向左右延伸水平线，碰到外边界(x=0或x=25)停
═══════════════════════════════════════════════════════════════════════════════════════

    x=0    5    7  8       13   16   18   21       25
     |     |    |  |       |    |    |    |         |
y=25 .─────┬────┬──┴────────┴────┴────┴────┴─────────┐
     │                                            │
     │              Zone F  (矩形 ~25×6)            │
     │           纯矩形区域,蛇形全宽横扫             │
     │                                            │
y=19 .─────┼────┼───────────────────────────┬───────┤  ← (16,19)右→25
     │     │    │          ┌─────────────┐  │       │     (18,5)做的水平线到这也停
y=18 .─────┼────┤    ┌─────┘             └──┼───────┤  ← (8,18)左→0
     │     │ E1 │    │     h  o  l  e      │  E3   │
     │  左 │    │    │                     │  右   │
     │  直 │    │    │                     │  直   │
y=13 .──┬──┴────┤    │    ┌────────────┬───┼───────┤  ← (21,13)右→25
     │  │      │    └────┘            │   │       │
y=12 .──┴──────┼──────────────────────┼───┼───────┤  ← (5,12)左→0
     │         │                      │   │       │
     │         │       Zone D  (矩形 ~25×7)        │
     │         │     纯矩形,全宽横扫               │
     │         │                                  │
y=6  .─────────┼──┬───────────────────┬──┼───────┤  ← (7,6)左→0
     │         │  │      hole         │  │       │
y=5  .─────────┼──┴───────────────────┴──┼───────┤  ← (18,5)右→25
     │         │                          │       │
     │         │       Zone B  (矩形 ~25×5)        │
     │         │     纯矩形,全宽横扫               │
     │                                            │
y=0  .─────────┴──────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════════════
分解结果: 6 个区域! (vs 当前 rectilinear 的 42 个) 每个区域都能跑完整宽度的 swath!
═══════════════════════════════════════════════════════════════════════════════════════

┌────────┬──────────────┬───────────────────────────────────────────────────┐
│ 区域   │ 形状          │ 说明                                              │
├────────┼──────────────┼───────────────────────────────────────────────────┤
│ Zone F │ 矩形 25×6     │ y=19→25, 全宽矩形, 12~13条水平swath              │
│ Zone E1│ 直角梯形      │ y=12→18, x范围: 左边界→孔洞斜边(8,18)→(5,12)     │
│ Zone E3│ 直角梯形      │ y=12→18, x范围: 孔洞斜边(21,13)→(16,19)→右边界   │
│ Zone D │ 矩形 25×7     │ y=5→12, 全宽矩形(孔洞在y=6-12不触左右边)          │
│ Zone B │ 矩形 25×5     │ y=0→5, 全宽矩形, 10~11条水平swath                │
└────────┴──────────────┴───────────────────────────────────────────────────┘

注意: 孔洞的 y=5→6 和 y=18→19 两条近水平边恰好形成 Zone B 上边界和 Zone F 下边界
      孔洞的 y=6→18 之间的区域被斜边分割为 E1(左下梯形) 和 E3(右上梯形)

═══════════════════════════════════════════════════════════════════════════════════════
蛇形规划路径 (0°水平扫, 从上到下 → 从下到上):
═══════════════════════════════════════════════════════════════════════════════════════

Zone F:  →→→→→→→→→→→→→→→→→→→→→→→→→  全宽横扫 13条
           ↓ (碰右边界,向下转)
Zone E3:  ←←←←←←←←←←←←  横扫到孔洞右斜边
           ↙ (贴孔洞斜边(16,19)→(21,13)滑到 E1)
Zone E1:  →→→→→→→→→→→→  从孔洞左斜边横扫到左边界
           ↓ (碰左边界,向下转)
Zone D:   ←←←←←←←←←←←←←←←←←←←←←←←←  全宽横扫 15条
           ↓
Zone B:   →→→→→→→→→→→→→→→→→→→→→→→→  全宽横扫 10条 到底!
           然后从下往上蛇形扫回来 (Zone B→D→E1→E3→F)

═══════════════════════════════════════════════════════════════════════════════════════
对比:
  当前 rectilinear: 42碎片, 400+ 掉头, 每个swath只跑4.5m
  老板方案:        6 区域, ~50 掉头, 每个swath跑满25m全宽
  路径质量:        天壤之别!
═══════════════════════════════════════════════════════════════════════════════════════
